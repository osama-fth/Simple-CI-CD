<!DOCTYPE html>
<html lang="it">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title><%= title %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.css" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <link rel="icon" href="data:,">
</head>

<body class="app-layout">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm py-2">
    <div class="container">
      <a class="navbar-brand fw-semibold d-flex align-items-center gap-2" href="/">
        <i class="bi bi-server text-primary"></i>
        <%= title %>
      </a>
      <div class="navbar-nav ms-auto">
        <span class="nav-link text-light small">
          <i class="bi bi-database me-1"></i>PostgreSQL
        </span>
      </div>
    </div>
  </nav>

  <main class="container py-5">
    <div class="row justify-content-center">
      <div class="col-xl-10 col-lg-11">
        <header class="text-center mb-4">
          <div class="d-flex justify-content-center align-items-center gap-2 mb-2">
            <i class="bi bi-people-fill text-primary fs-4"></i>
            <h1 class="h3 fw-semibold mb-0 page-title"><%= title %></h1>
          </div>
          <p class="text-muted mb-0">
            <i class="bi bi-gear me-1"></i>Gestione utenti demo con Node.js + PostgreSQL
          </p>
        </header>

        <section class="card shadow-soft border-0 overflow-hidden mb-4">
          <div class="card-header d-flex justify-content-between align-items-center py-3">
            <div class="d-flex align-items-center gap-2">
              <i class="bi bi-table text-primary"></i>
              <span class="fw-semibold">Tabella Utenti</span>
              <% if (users && users.length) { %>
                <span class="badge bg-primary-subtle text-primary">
                  <%= users.length %> record
                </span>
              <% } %>
            </div>
            <button class="btn btn-primary btn-sm d-flex align-items-center gap-1" 
                    data-bs-toggle="collapse" data-bs-target="#addUserForm" 
                    aria-expanded="false" aria-controls="addUserForm">
              <i class="bi bi-plus-circle"></i>
              <span class="d-none d-sm-inline">Nuovo Utente</span>
            </button>
          </div>

          <!-- Form collassabile -->
          <div class="collapse" id="addUserForm">
            <div class="card-body border-bottom bg-light">
              <form class="row g-2" action="/users" method="POST">
                <div class="col-md-3">
                  <div class="input-group input-group-sm">
                    <span class="input-group-text"><i class="bi bi-person"></i></span>
                    <input required name="nome" class="form-control" placeholder="Nome" />
                  </div>
                </div>
                <div class="col-md-3">
                  <div class="input-group input-group-sm">
                    <span class="input-group-text"><i class="bi bi-person-badge"></i></span>
                    <input required name="cognome" class="form-control" placeholder="Cognome" />
                  </div>
                </div>
                <div class="col-md-3">
                  <div class="input-group input-group-sm">
                    <span class="input-group-text"><i class="bi bi-envelope"></i></span>
                    <input required name="email" type="email" class="form-control" placeholder="Email" />
                  </div>
                </div>
                <div class="col-md-2">
                  <div class="input-group input-group-sm">
                    <span class="input-group-text"><i class="bi bi-calendar-date"></i></span>
                    <input name="data_di_nascita" type="date" class="form-control" />
                  </div>
                </div>
                <div class="col-md-1">
                  <button type="submit" class="btn btn-success btn-sm w-100">
                    <i class="bi bi-check-lg"></i>
                  </button>
                </div>
              </form>
            </div>
          </div>

          <div class="card-body p-0">
            <% if (users && users.length) { %>
              <div class="table-responsive">
                <table class="table table-hover table-sm align-middle mb-0 table-modern">
                  <thead>
                    <tr>
                      <th class="text-center">
                        <i class="bi bi-hash"></i>
                      </th>
                      <th>
                        <i class="bi bi-person me-1"></i>Nome
                      </th>
                      <th>
                        <i class="bi bi-person-badge me-1"></i>Cognome
                      </th>
                      <th>
                        <i class="bi bi-envelope me-1"></i>Email
                      </th>
                      <th>
                        <i class="bi bi-calendar-date me-1"></i>Nascita
                      </th>
                      <th class="text-center">
                        <i class="bi bi-gear"></i>
                      </th>
                    </tr>
                  </thead>
                  <tbody>
                    <% users.forEach(u => { %>
                      <tr>
                        <td class="text-center">
                          <code class="id-badge"><%= u.id %></code>
                        </td>
                        <td class="fw-medium">
                          <i class="bi bi-person-circle text-muted me-2"></i><%= u.nome %>
                        </td>
                        <td><%= u.cognome %></td>
                        <td>
                          <div class="d-flex align-items-center gap-1">
                            <i class="bi bi-at text-muted"></i>
                            <span class="email-chip"><%= u.email %></span>
                          </div>
                        </td>
                        <td>
                          <% if (u.data_di_nascita) { %>
                            <i class="bi bi-calendar3 text-muted me-1"></i><%= u.data_di_nascita %>
                          <% } else { %>
                            <span class="text-muted">—</span>
                          <% } %>
                        </td>
                        <td class="text-center">
                          <form action="/users/<%= u.id %>/delete" method="POST" 
                                onsubmit="return confirm('Eliminare definitivamente l\'utente <%= u.nome %> <%= u.cognome %>?')" 
                                class="d-inline">
                            <button type="submit" class="btn btn-outline-danger btn-sm px-2 py-1" 
                                    title="Elimina utente">
                              <i class="bi bi-trash"></i>
                            </button>
                          </form>
                        </td>
                      </tr>
                    <% }) %>
                  </tbody>
                </table>
              </div>
            <% } else { %>
              <div class="p-5 text-center">
                <div class="mb-3">
                  <i class="bi bi-people display-1 text-muted opacity-50"></i>
                </div>
                <h5 class="fw-semibold mb-2">Nessun utente presente</h5>
                <p class="text-muted mb-3">La tabella è vuota. Aggiungi il primo utente per iniziare.</p>
                <button class="btn btn-primary btn-sm" 
                        data-bs-toggle="collapse" data-bs-target="#addUserForm">
                  <i class="bi bi-plus-circle me-1"></i>Aggiungi Utente
                </button>
              </div>
            <% } %>
          </div>
        </section>

        <footer class="text-center py-3">
          <div class="d-flex justify-content-center align-items-center gap-3 text-muted small">
            <span class="d-flex align-items-center gap-1">
              <i class="bi bi-code-slash"></i>Demo DevSecOps
            </span>
            <span class="d-flex align-items-center gap-1">
              <i class="bi bi-layers"></i>
              <span class="stack-ref">Express + PostgreSQL</span>
            </span>
            <span class="d-flex align-items-center gap-1">
              <i class="bi bi-github"></i>CI/CD Pipeline
            </span>
          </div>
        </footer>
      </div>
    </div>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
</body>

</html>
