<!DOCTYPE html>
<html lang="it">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>
    <%= title %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
</head>

<body class="app-layout">
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark shadow-sm py-2">
    <div class="container">
      <a class="navbar-brand fw-semibold d-flex align-items-center gap-2" href="#">
        <span class="brand-accent"></span>
        <%= title %>
      </a>
    </div>
  </nav>

  <main class="container py-5 fade-in">
    <div class="row justify-content-center">
      <div class="col-xl-9 col-lg-10">
        <header class="text-center mb-4">
          <h1 class="h3 fw-semibold mb-2 page-title">
            <%= title %>
          </h1>
          <p class="text-muted mb-0">Gestione utenti demo con Node.js + Postgres</p>
        </header>

        <section class="card shadow-soft border-0 overflow-hidden mb-4">
          <div class="card-header bg-surface d-flex justify-content-between align-items-center py-3 px-4">
            <div class="d-flex align-items-center gap-2">
              <span class="badge rounded-pill bg-tag text-primary fw-semibold small px-3">Utenti</span>
              <span class="fw-semibold">Tabella</span>
            </div>
            <% if (users && users.length) { %>
              <span class="text-secondary small">
                <strong>
                  <%= users.length %>
                </strong> record
              </span>
              <% } %>
          </div>
          <div class="card-body p-0">
            <% if (users && users.length) { %>
              <div class="table-responsive">
                <table class="table table-hover table-sm align-middle mb-0 table-modern">
                  <thead>
                    <tr>
                      <th>ID</th>
                      <th>Nome</th>
                      <th>Cognome</th>
                      <th>Email</th>
                      <th>Data di Nascita</th>
                    </tr>
                  </thead>
                  <tbody>
                    <% users.forEach(user=> { %>
                      <tr>
                        <td><code class="id-badge"><%= user.id %></code></td>
                        <td class="fw-medium">
                          <%= user.nome %>
                        </td>
                        <td>
                          <%= user.cognome %>
                        </td>
                        <td><span class="email-chip">
                            <%= user.email %>
                          </span></td>
                        <td>
                          <%= user.data_di_nascita %>
                        </td>
                      </tr>
                      <% }) %>
                  </tbody>
                </table>
              </div>
              <% } else { %>
                <div class="p-5 text-center">
                  <div class="empty-state-icon mb-3">👥</div>
                  <p class="lead mb-1 fw-semibold">Nessun utente presente</p>
                  <p class="text-muted small mb-0">Popola la tabella inizializzando il database.</p>
                </div>
                <% } %>
          </div>
        </section>

        <footer class="app-footer small text-center text-muted py-3">
          <span>Demo • <span class="stack-ref">Express + PostgreSQL</span></span>
        </footer>
      </div>
    </div>
  </main>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
    integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
    crossorigin="anonymous"></script>
</body>

</html>
